package zShortcut;

public class Main {

	public static void main(String[] args) {
//		int x = Integer.parseInt(str); //string -> int
//		String s = String.valueOf(str); //int -> string
//		double d = String.format("%.2f", vol); // 2.434506 -> 2.43
		
		/*double sum = a+b+c; // triangle area
		double max = Math.max(Math.max(a, b), c);
		if(max >= sum-max)
			System.out.print("Area can't possible for this side.");
		else {
			double s = (sum)/2.0;
			System.out.println("Triangle Area :: "+(Math.sqrt(s*(s-a)*(s-b)*(s-c))));
		}*/
		
		
	}

}
